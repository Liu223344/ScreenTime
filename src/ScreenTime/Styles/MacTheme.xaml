<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <!-- ============== 颜色(Apple Design System) ============== -->
    <Color x:Key="WindowBgColor">#ECECEC</Color>
    <Color x:Key="SidebarBgColor">#F5F5F7</Color>
    <Color x:Key="CardBgColor">#FFFFFF</Color>
    <Color x:Key="CardHoverColor">#FAFAFA</Color>

    <Color x:Key="TextColorPrimary">#1D1D1F</Color>
    <Color x:Key="TextColorSecondary">#6E6E73</Color>
    <Color x:Key="TextColorTertiary">#A1A1A6</Color>

    <!-- Apple 系统强调色 -->
    <Color x:Key="AccentColor">#007AFF</Color>
    <Color x:Key="BlueColor">#007AFF</Color>
    <Color x:Key="GreenColor">#34C759</Color>
    <Color x:Key="OrangeColor">#FF9500</Color>
    <Color x:Key="PurpleColor">#AF52DE</Color>
    <Color x:Key="RedColor">#FF3B30</Color>
    <Color x:Key="TealColor">#5AC8FA</Color>
    <Color x:Key="YellowColor">#FFCC00</Color>
    <Color x:Key="PinkColor">#FF2D55</Color>

    <SolidColorBrush x:Key="WindowBg" Color="{StaticResource WindowBgColor}" />
    <SolidColorBrush x:Key="SidebarBg" Color="{StaticResource SidebarBgColor}" />
    <SolidColorBrush x:Key="CardBg" Color="{StaticResource CardBgColor}" />
    <SolidColorBrush x:Key="CardHoverBg" Color="{StaticResource CardHoverColor}" />

    <SolidColorBrush x:Key="TextPrimary" Color="{StaticResource TextColorPrimary}" />
    <SolidColorBrush x:Key="TextSecondary" Color="{StaticResource TextColorSecondary}" />
    <SolidColorBrush x:Key="TextTertiary" Color="{StaticResource TextColorTertiary}" />

    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}" />
    <SolidColorBrush x:Key="BlueBrush" Color="{StaticResource BlueColor}" />
    <SolidColorBrush x:Key="GreenBrush" Color="{StaticResource GreenColor}" />
    <SolidColorBrush x:Key="OrangeBrush" Color="{StaticResource OrangeColor}" />
    <SolidColorBrush x:Key="PurpleBrush" Color="{StaticResource PurpleColor}" />
    <SolidColorBrush x:Key="RedBrush" Color="{StaticResource RedColor}" />
    <SolidColorBrush x:Key="TealBrush" Color="{StaticResource TealColor}" />
    <SolidColorBrush x:Key="YellowBrush" Color="{StaticResource YellowColor}" />

    <SolidColorBrush x:Key="DividerBrush" Color="#E5E5EA" />
    <SolidColorBrush x:Key="HairlineBrush" Color="#EDEDF0" />

    <!-- ============== 字体 ============== -->
    <!-- SF Pro 在 Windows 上没有,回退到 Segoe UI Variable / Segoe UI,视觉上接近 -->
    <FontFamily x:Key="SFFontFamily">Segoe UI Variable Text, Segoe UI, SF Pro Text, -apple-system, Helvetica Neue, Arial</FontFamily>
    <FontFamily x:Key="SFDisplayFamily">Segoe UI Variable Display, Segoe UI, SF Pro Display, -apple-system, Helvetica Neue, Arial</FontFamily>

    <sys:Double x:Key="CaptionSize">11</sys:Double>
    <sys:Double x:Key="FootnoteSize">12</sys:Double>
    <sys:Double x:Key="SubheadlineSize">13</sys:Double>
    <sys:Double x:Key="BodySize">14</sys:Double>
    <sys:Double x:Key="HeadlineSize">15</sys:Double>
    <sys:Double x:Key="TitleSize">17</sys:Double>
    <sys:Double x:Key="LargeTitleSize">26</sys:Double>
    <sys:Double x:Key="NumberHeroSize">34</sys:Double>

    <!-- ============== 阴影(柔和、低不透明度,模拟 macOS) ============== -->
    <DropShadowEffect x:Key="CardShadow"
                      Color="#000000" BlurRadius="20" ShadowDepth="0" Opacity="0.08" Direction="270" />
    <DropShadowEffect x:Key="CardShadowHover"
                      Color="#000000" BlurRadius="28" ShadowDepth="2" Opacity="0.12" Direction="270" />
    <DropShadowEffect x:Key="TrafficLightHover"
                      Color="#000000" BlurRadius="6" ShadowDepth="0" Opacity="0.25" />

    <!-- ============== 圆角 ============== -->
    <CornerRadius x:Key="CardCornerRadius">12</CornerRadius>
    <CornerRadius x:Key="ButtonCornerRadius">8</CornerRadius>
    <CornerRadius x:Key="PillCornerRadius">10</CornerRadius>

    <!-- ============== 基础字体样式 ============== -->
    <Style TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource SFFontFamily}" />
        <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
        <Setter Property="FontSize" Value="{StaticResource BodySize}" />
        <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
        <Setter Property="TextOptions.TextRenderingMode" Value="ClearType" />
    </Style>

    <Style x:Key="CaptionText" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource SFFontFamily}" />
        <Setter Property="FontSize" Value="{StaticResource CaptionSize}" />
        <Setter Property="Foreground" Value="{StaticResource TextSecondary}" />
    </Style>

    <Style x:Key="SecondaryText" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource SFFontFamily}" />
        <Setter Property="FontSize" Value="{StaticResource FootnoteSize}" />
        <Setter Property="Foreground" Value="{StaticResource TextSecondary}" />
    </Style>

    <Style x:Key="SectionTitle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource SFDisplayFamily}" />
        <Setter Property="FontSize" Value="{StaticResource HeadlineSize}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
    </Style>

    <Style x:Key="LargeTitle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource SFDisplayFamily}" />
        <Setter Property="FontSize" Value="{StaticResource LargeTitleSize}" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
    </Style>

    <Style x:Key="HeroNumber" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource SFDisplayFamily}" />
        <Setter Property="FontSize" Value="{StaticResource NumberHeroSize}" />
        <Setter Property="FontWeight" Value="Bold" />
    </Style>

    <!-- ============== 卡片样式 ============== -->
    <Style x:Key="MacCard" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource CardBg}" />
        <Setter Property="CornerRadius" Value="{StaticResource CardCornerRadius}" />
        <Setter Property="Effect" Value="{StaticResource CardShadow}" />
        <Setter Property="Padding" Value="16" />
    </Style>

    <!-- ============== 主按钮(Apple Blue) ============== -->
    <Style x:Key="PrimaryButton" TargetType="Button">
        <Setter Property="FontFamily" Value="{StaticResource SFFontFamily}" />
        <Setter Property="FontSize" Value="{StaticResource BodySize}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="Background" Value="{StaticResource AccentBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="18,8" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd"
                            Background="{TemplateBinding Background}"
                            CornerRadius="{StaticResource ButtonCornerRadius}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Opacity" Value="0.88" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Opacity" Value="0.7" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="bd" Property="Opacity" Value="0.4" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============== 次按钮(浅灰) ============== -->
    <Style x:Key="SecondaryButton" TargetType="Button">
        <Setter Property="FontFamily" Value="{StaticResource SFFontFamily}" />
        <Setter Property="FontSize" Value="{StaticResource BodySize}" />
        <Setter Property="FontWeight" Value="Medium" />
        <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
        <Setter Property="Background" Value="#FFFFFF" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="16,8" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd"
                            Background="{TemplateBinding Background}"
                            CornerRadius="{StaticResource ButtonCornerRadius}"
                            Padding="{TemplateBinding Padding}">
                        <Border.Effect>
                            <DropShadowEffect Color="#000000" BlurRadius="12" ShadowDepth="0" Opacity="0.06" />
                        </Border.Effect>
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="#F5F5F7" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="#E5E5EA" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============== 复选框(macOS 风格:小方块 + 圆角,勾选时蓝底白勾) ============== -->
    <Style x:Key="MacCheckbox" TargetType="CheckBox">
        <Setter Property="FontFamily" Value="{StaticResource SFFontFamily}" />
        <Setter Property="FontSize" Value="{StaticResource BodySize}" />
        <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal">
                        <Border x:Name="box" Width="18" Height="18"
                                CornerRadius="5"
                                Background="White"
                                BorderBrush="#D1D1D6" BorderThickness="1.5"
                                VerticalAlignment="Center">
                            <Path x:Name="check"
                                  Stretch="Uniform"
                                  Margin="3"
                                  Data="M 0 5 L 4 9 L 10 0"
                                  Stroke="White" StrokeThickness="2"
                                  StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                  Visibility="Collapsed" />
                        </Border>
                        <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center" RecognizesAccessKey="True" />
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="box" Property="Background" Value="{StaticResource AccentBrush}" />
                            <Setter TargetName="box" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                            <Setter TargetName="check" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="box" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============== TextBox ============== -->
    <Style x:Key="MacTextBox" TargetType="TextBox">
        <Setter Property="FontFamily" Value="{StaticResource SFFontFamily}" />
        <Setter Property="FontSize" Value="{StaticResource BodySize}" />
        <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
        <Setter Property="Background" Value="#F5F5F7" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="10,6" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="bd"
                            Background="{TemplateBinding Background}"
                            CornerRadius="6"
                            BorderBrush="Transparent" BorderThickness="1"
                            Padding="{TemplateBinding Padding}">
                        <ScrollViewer x:Name="PART_ContentHost" Focusable="False"
                                      HorizontalScrollBarVisibility="Hidden"
                                      VerticalScrollBarVisibility="Hidden" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                            <Setter TargetName="bd" Property="Background" Value="White" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============== GroupBox ============== -->
    <Style x:Key="MacGroupBox" TargetType="GroupBox">
        <Setter Property="FontFamily" Value="{StaticResource SFDisplayFamily}" />
        <Setter Property="FontSize" Value="{StaticResource HeadlineSize}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="16,12,16,16" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="GroupBox">
                    <Border Background="White" CornerRadius="12" Padding="{TemplateBinding Padding}">
                        <Border.Effect>
                            <DropShadowEffect Color="#000000" BlurRadius="18" ShadowDepth="0" Opacity="0.06" />
                        </Border.Effect>
                        <StackPanel>
                            <ContentPresenter ContentSource="Header" Margin="0,0,0,10" />
                            <ContentPresenter />
                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============== DataGrid ============== -->
    <Style x:Key="MacDataGrid" TargetType="DataGrid">
        <Setter Property="FontFamily" Value="{StaticResource SFFontFamily}" />
        <Setter Property="FontSize" Value="{StaticResource BodySize}" />
        <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="RowBackground" Value="Transparent" />
        <Setter Property="AlternatingRowBackground" Value="Transparent" />
        <Setter Property="GridLinesVisibility" Value="Horizontal" />
        <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource HairlineBrush}" />
        <Setter Property="HeadersVisibility" Value="Column" />
        <Setter Property="RowHeight" Value="36" />
        <Setter Property="ColumnHeaderHeight" Value="36" />
        <Setter Property="CellStyle">
            <Setter.Value>
                <Style TargetType="DataGridCell">
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Focusable" Value="False" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border Padding="0" Background="Transparent">
                                    <ContentPresenter VerticalAlignment="Center" />
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
        <Setter Property="ColumnHeaderStyle">
            <Setter.Value>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="FontFamily" Value="{StaticResource SFFontFamily}" />
                    <Setter Property="FontSize" Value="{StaticResource FootnoteSize}" />
                    <Setter Property="FontWeight" Value="SemiBold" />
                    <Setter Property="Foreground" Value="{StaticResource TextSecondary}" />
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="BorderThickness" Value="0,0,0,1" />
                    <Setter Property="BorderBrush" Value="{StaticResource HairlineBrush}" />
                    <Setter Property="Padding" Value="12,0" />
                    <Setter Property="Height" Value="36" />
                </Style>
            </Setter.Value>
        </Setter>
        <Setter Property="RowHeaderWidth" Value="0" />
    </Style>

    <!-- ============== Slider(macOS 风格:细轨道 + 圆形 thumb) ============== -->
    <Style x:Key="MacSlider" TargetType="Slider">
        <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
        <Setter Property="Height" Value="28" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Slider">
                    <Grid>
                        <Border Height="6" VerticalAlignment="Center" CornerRadius="3" Background="#E5E5EA" />
                        <Track x:Name="PART_Track" VerticalAlignment="Center">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="Slider.DecreaseLarge">
                                    <RepeatButton.Template>
                                        <ControlTemplate TargetType="RepeatButton">
                                            <Border Height="6" CornerRadius="3" Background="{StaticResource AccentBrush}" />
                                        </ControlTemplate>
                                    </RepeatButton.Template>
                                </RepeatButton>
                            </Track.DecreaseRepeatButton>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="Slider.IncreaseLarge">
                                    <RepeatButton.Template>
                                        <ControlTemplate TargetType="RepeatButton">
                                            <Border Background="Transparent" />
                                        </ControlTemplate>
                                    </RepeatButton.Template>
                                </RepeatButton>
                            </Track.IncreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb>
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="Thumb">
                                            <Grid Width="22" Height="22">
                                                <Ellipse Fill="White">
                                                    <Ellipse.Effect>
                                                        <DropShadowEffect Color="#000000" BlurRadius="6" ShadowDepth="1" Opacity="0.25" />
                                                    </Ellipse.Effect>
                                                </Ellipse>
                                                <Ellipse Fill="White" Stroke="{StaticResource AccentBrush}" StrokeThickness="0" />
                                            </Grid>
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============== Segmented Control Item(macOS 分段选择器单项) ============== -->
    <Style x:Key="SegmentedItem" TargetType="RadioButton">
        <Setter Property="FontFamily" Value="{StaticResource SFFontFamily}" />
        <Setter Property="FontSize" Value="{StaticResource FootnoteSize}" />
        <Setter Property="FontWeight" Value="Medium" />
        <Setter Property="Foreground" Value="{StaticResource TextSecondary}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Border x:Name="bd"
                            Background="Transparent"
                            CornerRadius="6"
                            Margin="3,2"
                            Padding="0">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                          RecognizesAccessKey="True" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="#DADADD" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="White" />
                            <Setter TargetName="bd" Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect Color="#000000" BlurRadius="8" ShadowDepth="0" Opacity="0.08" />
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
                            <Setter Property="FontWeight" Value="SemiBold" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============== 滚动条(细、悬浮、macOS 风格) ============== -->
    <Style TargetType="ScrollBar">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Width" Value="10" />
        <Setter Property="MinWidth" Value="10" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Grid>
                        <Track Name="PART_Track" IsDirectionReversed="True">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="ScrollBar.PageUpCommand">
                                    <RepeatButton.Template>
                                        <ControlTemplate TargetType="RepeatButton">
                                            <Border Background="Transparent" />
                                        </ControlTemplate>
                                    </RepeatButton.Template>
                                </RepeatButton>
                            </Track.DecreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb>
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="Thumb">
                                            <Border Background="#BDBDBD" CornerRadius="5" Margin="2,0" Opacity="0.7">
                                                <Border.Effect>
                                                    <DropShadowEffect Color="#000000" BlurRadius="4" ShadowDepth="0" Opacity="0.15" />
                                                </Border.Effect>
                                            </Border>
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="ScrollBar.PageDownCommand">
                                    <RepeatButton.Template>
                                        <ControlTemplate TargetType="RepeatButton">
                                            <Border Background="Transparent" />
                                        </ControlTemplate>
                                    </RepeatButton.Template>
                                </RepeatButton>
                            </Track.IncreaseRepeatButton>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Height" Value="10" />
                <Setter Property="MinHeight" Value="10" />
                <Setter Property="Width" Value="auto" />
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
